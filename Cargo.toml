[package]
name = "dl-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.1", features = ["dynamic_linking", "debug", "free_camera", "experimental_bevy_feathers"] }
rand = { version = "0.10.1" }
transform-gizmo-bevy = { version = "0.9.0" }
bevy_simple_text_input = "0.14.2"

[profile.dev.package."*"]
opt-level = 3