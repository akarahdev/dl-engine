[package]
name = "dl-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.1", features = ["dynamic_linking", "debug", "free_camera"] }
rand = { version = "0.10.1" }
transform-gizmo-bevy = { version = "0.9.0" }

[profile.dev.package."*"]
opt-level = 3